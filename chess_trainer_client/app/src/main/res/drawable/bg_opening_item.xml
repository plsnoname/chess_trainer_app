<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state: button primary with border -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_primary" />
            <corners android:radius="12dp" />
            <stroke
                android:width="2dp"
                android:color="@color/button_primary_hover" />
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_primary_hover" />
            <corners android:radius="12dp" />
            <stroke
                android:width="2dp"
                android:color="@color/button_primary" />
        </shape>
    </item>
    <!-- Default state: card surface background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/card_surface" />
            <corners android:radius="12dp" />
            <stroke
                android:width="1dp"
                android:color="@color/button_primary" />
        </shape>
    </item>
</selector>
